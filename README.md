# Health-checks
Scripts that check the health of my computer


This repo will be populated with lots of fancy checks.



Navigate to the health-checks directory

cd health-checks/

Main script: health_checks.py

This script prints "Everything ok" if all checks pass,

or prints error messages if some checks fail.

Example content of health_checks.py (simplified)
.
